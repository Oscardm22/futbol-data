<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FutbolData"
        tools:targetApi="31">
        <activity
            android:name=".ui.jugadores.AddJugadorActivity"
            android:exported="false" />
        <activity
            android:name=".ui.competiciones.CompeticionesActivity"
            android:exported="false" />
        <activity
            android:name=".ui.partidos.AddPartidoActivity"
            android:exported="false" />
        <activity
            android:name=".ui.equipos.EquipoDetailActivity"
            android:exported="false" /> <!-- SplashActivity como actividad principal -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.App.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- LoginActivity -->
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="false"
            android:theme="@style/Theme.FutbolData" /> <!-- Otras actividades -->
        <activity
            android:name=".ui.equipos.EquiposActivity"
            android:exported="false" />
    </application>

</manifest>