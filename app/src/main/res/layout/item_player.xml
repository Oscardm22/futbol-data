<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="8dp"
    android:layout_marginVertical="4dp"
    app:cardCornerRadius="8dp"
    app:cardElevation="2dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="12dp"
        android:background="?attr/selectableItemBackground">

        <ImageView
            android:id="@+id/ivPositionIcon"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_gravity="center"
            android:contentDescription="@string/position_icon_desc"
            android:scaleType="centerInside"
            android:padding="8dp"
            tools:src="@drawable/ic_position_default"/>

        <!-- Contenedor de información principal -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="16dp"
            android:orientation="vertical">

            <!-- Nombre del jugador -->
            <TextView
                android:id="@+id/tvNombre"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textSize="16sp"
                android:textColor="?android:attr/textColorPrimary"
                android:textStyle="bold"
                android:ellipsize="end"
                android:maxLines="1"
                tools:text="Alaba" />

            <!-- Contenedor de detalles (posición y estadísticas) -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="6dp"
                android:orientation="horizontal"
                android:gravity="center_vertical">

                <!-- Badge de posición -->
                <TextView
                    android:id="@+id/tvPosicion"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:paddingHorizontal="8dp"
                    android:paddingVertical="2dp"
                    android:background="@drawable/bg_position_badge"
                    android:textSize="12sp"
                    android:textColor="@color/white"
                    android:textAllCaps="true"
                    android:textStyle="bold"
                    tools:text="DFC" />

                <!-- Estadísticas compactas con compound drawables -->
                <LinearLayout
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="4dp"
                    android:orientation="horizontal"
                    android:gravity="center">

                    <!-- Partidos jugados -->
                    <TextView
                        android:id="@+id/tvPartidosJugados"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginEnd="4dp"
                        android:drawablePadding="1dp"
                        android:textSize="11sp"
                        android:textColor="?android:attr/textColorSecondary"
                        android:textStyle="bold"
                        app:drawableStartCompat="@drawable/ic_games"
                        tools:text="6" />

                <!-- Goles -->
                <TextView
                    android:id="@+id/tvGoles"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginEnd="4dp"
                    android:drawablePadding="1dp"
                    android:textSize="11sp"
                    android:textColor="?android:attr/textColorSecondary"
                    android:textStyle="bold"
                    app:drawableStartCompat="@drawable/ic_gol"
                    tools:text="1" />

                <!-- Asistencias -->
                <TextView
                    android:id="@+id/tvAsistencias"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginEnd="4dp"
                    android:drawablePadding="1dp"
                    android:textSize="11sp"
                    android:textColor="?android:attr/textColorSecondary"
                    android:textStyle="bold"
                    app:drawableStartCompat="@drawable/ic_asistencia"
                    tools:text="1" />

                <!-- MVP -->
                <TextView
                    android:id="@+id/tvMVP"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginEnd="4dp"
                    android:drawablePadding="1dp"
                    android:textSize="11sp"
                    android:textColor="?android:attr/textColorSecondary"
                    android:textStyle="bold"
                    app:drawableStartCompat="@drawable/ic_mvp_selected"
                    tools:text="0" />
                </LinearLayout>
            </LinearLayout>
        </LinearLayout>

        <ImageView
            android:id="@+id/ivDelete"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_gravity="center"
            android:contentDescription="@string/desc_btnDelete"
            android:src="@drawable/ic_delete"/>
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>